##
# Makefile automatically generated by genmake 1.0, Sep-22-02
# genmake 1.0 by ma_muquit@fccc.edu
##
CC= cc
DEFS=  
PROGNAME= hopper
INCLUDES=  -I.
LIBS=


# replace -O with -g in order to debug

DEFINES= $(INCLUDES) $(DEFS) -DSYS_UNIX=1
CFLAGS= -O $(DEFINES)

SRCS = add.c convert.c dhcp.c getinfo.c hopper.c internet.c link.c

OBJS = add.o convert.o dhcp.o getinfo.o hopper.o internet.o link.o

.c.o:
	rm -f $@
	$(CC) $(CFLAGS) -c $*.c

all: $(PROGNAME)

$(PROGNAME) : $(OBJS)
	$(CC) $(CFLAGS) -o $(PROGNAME) $(OBJS) $(LIBS)

clean:
	rm -f $(OBJS) $(PROGNAME) core
